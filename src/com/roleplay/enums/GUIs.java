package com.roleplay.enums;

/**
 * This enum has a list of all the possible GUIs which can be called onto the current scene
 */
public enum GUIs {
    CHARACTER, DAMAGE, HEALTH, MAGIC_USE, MAGIC_RESTORE, EXPERIENCE, SAVING, VALUE_SETTING
}
